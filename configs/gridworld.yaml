env:
  type: gridworld
  grid_size: 8
  num_objects: 5

brain:
  latent_dim: 32
  hidden_dim: 256
  n_concepts: 6

training:
  episodes: 2000
  max_steps: 200
  batch_size: 64
  buffer_size: 50000
  curiosity_rate: 0.3
  plan_every: 15
  plan_len: 3
  k_plans: 4
  cluster_every: 100
  sleep_every: 200
  check_every: 50
  seed: 42
